{
	"name": "discord_bot",
	"version": "1.0.0",
	"description": "A sample trial for creating a discord bot.",
	"main": "index.js",
	"scripts": {
		"run": "node deploy-commands.js && node index.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "k8pai",
	"license": "ISC",
	"dependencies": {
		"discord.js": "^14.7.1",
		"dotenv": "^16.0.3"
	},
	"devDependencies": {
		"eslint": "^8.34.0"
	}
}
